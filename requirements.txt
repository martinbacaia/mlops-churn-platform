# Runtime dependencies (pinned for reproducibility).
# More deps are added as modules are introduced; this file grows commit-by-commit.
#
# PyTorch is intentionally CPU-only — keeps the repo clonable on any machine and
# CI green without GPU runners. Install it from PyTorch's CPU index:
#   pip install torch==2.7.1 --index-url https://download.pytorch.org/whl/cpu
# (the +cpu local-version tag is set by that index and not portable to PyPI).
structlog==24.4.0
pydantic==2.10.3
pydantic-settings==2.6.1
python-dotenv==1.0.1
numpy==1.26.4
pandas==2.2.3
pandera==0.20.4
scikit-learn==1.5.2
xgboost==2.1.3
requests==2.32.3
joblib==1.5.3
torch==2.7.1
mlflow==2.18.0
optuna==4.1.0
fastapi==0.115.6
uvicorn==0.34.0
httpx==0.28.1
